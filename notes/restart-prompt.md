# Restart Prompt

Run `startup context` for:

`C:\Users\isen\OneDrive\Documents\Code Files\inc-stats-tracker`

Read `AGENTS.md`, `README.md`, `PROJECT_HEALTH.md`, `notes/current-state.md`,
`notes/task-registry.md`, `notes/continuation-brief.md`, and the `1.0.9` task/results packet.

First read `Last Conversation Exchange — Preserve for Exact Startup Recovery` in
`notes/continuation-brief.md`. Restate the preserved last startup prompt and its complete recovered
results, then compare file timestamps and report any newer evidence. Do not claim the pre-startup
interrupted exchange was literally available; the handoff records that it was unavailable and gives
the evidence-backed reconstruction returned in the last session.

Resume the `1.0.9` three-card Group Stats KPI workstream. Development runs active `1.0.9` with the
approved rollback and bounded smoke evidence; Production remains active on `1.0.8`.
Complete human Development Group Stats visual acceptance. Production remains a later separate gate.

Do not deploy, mutate either WordPress environment, expose credentials/member data, delete remote
archives or historical CSVs, commit, push, tag, or rewrite history without separate authorization.
Any further deployment or remote mutation requires a fresh exact gate.
