# CLAUDE.md — INC Stats Tracker

Start by reading `AGENTS.md`. Treat it as the shared source of truth for Codex, Claude, and other agents.

Before starting a task, check whether project skills in `.claude/skills/` apply. Use them when relevant —
`inc-project-init` covers startup-context review for this repo.
